# Django Framework (LTS version per Architecture requirement)
Django==5.2 

# Environment variable management
python-decouple==3.8

# MySQL database driver (for production - MySQL 8.0+)
# Story 2.1: Switched from SQLite to MySQL
mysqlclient==2.2.7

# File upload security (Story 2.8)
# python-magic==0.4.27  # MIME type detection - requires libmagic system dependency
# Note: Install python-magic-bin on Windows OR use fallback to content_type header
# Production Linux servers should have libmagic installed by default

# PDF Generation (Story 2.13)
reportlab==4.0.7

# Future dependencies (will be added in later stories)
# Celery==5.3.0  # Epic 2 - Email notifications, background tasks
